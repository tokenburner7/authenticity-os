{
  "name": "authenticity-os",
  "version": "0.0.1",
  "private": true,
  "description": "Authenticity protocol + AI personal agent platform for the post-tribal digital native",
  "license": "MIT",
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
